<h1>編集画面</h1>
<%= form_with model:@list, url:"/todolists/#{@list.id}", local:true do |f| %>
  <p>タイトル</p>
  <%= f.text_field :title %>
  <p>本文</p>
  <%= f.text_area :body %>
  <%= f.submit '保存' %>
<% end %>